<form method="post" action="{{ path('swapper_delete', {'id': swapper.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ swapper.id) }}">
    <button class="btn">Delete</button>
</form>
