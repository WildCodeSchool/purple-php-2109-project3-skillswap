    {{ form_start(form) }}
        <button type="submit" class="btnModifProfile btn btn-outline-danger rounded-pill">Valider vos compétences</button>
        {{ form_widget(form) }}
    {{ form_end(form) }}