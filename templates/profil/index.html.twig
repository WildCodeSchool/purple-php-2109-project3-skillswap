{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <h1>Profil</h1>
    <ul>
    {% for skill in user.skills|slice(0, 5) %}
    <li>{{ skill.name }}</li>
    {% endfor %}
    </ul>
    {% include "profil/_usersSkillsForm.html.twig" %}
{% endblock %}